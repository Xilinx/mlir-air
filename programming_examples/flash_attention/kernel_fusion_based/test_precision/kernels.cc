// Diagnostic kernel functions for flash attention precision testing.
// Reuses the same code as attn.cc by including it directly.
// This ensures we test the exact same kernel functions.
#include "../attn.cc"
