# Copyright (C) 2026, Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT
#
# Nightly LLM end-to-end performance benchmark on the dedicated Krackan Point
# runner (Ryzen AI 5 PRO 340, label `amdryzenai5pro340`). Builds mlir-air once,
# then for every model in BENCH_MODELS runs the correctness gate (verify) and
# captures perf (profile), collecting TTFT + decode tok/s into perf.json and
# uploading it as an artifact. Kept out of the per-PR matrix so perf numbers are
# contention-free. The job goes red if any model's compile/verify fails.

name: Nightly LLM Perf Benchmark

on:
  schedule:
    - cron: '17 4 * * *'  # Daily at 04:17 (off-:00 to avoid CI fleet pile-up)
  workflow_dispatch:

defaults:
  run:
    shell: bash

concurrency:
  group: nightly-perf-benchmark
  cancel-in-progress: false

env:
  DEBIAN_FRONTEND: noninteractive
  XILINXD_LICENSE_FILE: /opt/xilinx/Xilinx.lic
  VITIS: /opt/ryzen_ai-1.3.0.1/vitis_aie_essentials
  # Pinned run params shared across steps (single source of truth). Models are
  # data, not fixed names — extend BENCH_MODELS to cover more without renaming.
  # Space-separated list of model dirs under programming_examples/llms/.
  BENCH_MODELS: "llama32_1b llama32_1b_int4 llama32_3b qwen25_0_5b qwen25_1_5b qwen25_3b qwen3_0_6b qwen3_1_7b qwen3_4b smollm2_1_7b"
  BENCH_PROMPT: What is the capital of France?
  BENCH_N_TOKENS: "128"

jobs:
  benchmark:
    name: LLM perf benchmark
    runs-on: amdryzenai5pro340
    # Bound total runtime so stuck NPU dispatch/downloads can't occupy the
    # dedicated runner indefinitely. Covers a full build + all models (each
    # model is also individually bounded by `timeout` in run_models.sh).
    timeout-minutes: 240

    steps:

      - uses: actions/checkout@v3
        with:
          submodules: "true"

      - name: Setup environment
        run: |
          python3.12 -m venv air-venv
          source air-venv/bin/activate
          pip cache purge
          pip install --upgrade pip
          pip install lit cmake joblib
          pip install --pre torch-mlir torchvision \
            --index-url https://download.pytorch.org/whl/nightly/cpu \
            -f https://github.com/llvm/torch-mlir-release/releases/expanded_assets/dev-wheels

      - name: Get mlir-aie
        run: |
          utils/clone-mlir-aie.sh
          source air-venv/bin/activate
          pushd mlir-aie
          pip install -r python/requirements.txt
          pip install -r python/requirements_ml.txt
          pip install "torchvision==0.27.1+cpu" --index-url https://download.pytorch.org/whl/cpu
          pip install -r python/requirements_dev.txt

          VERSION=$(utils/clone-llvm.sh --get-wheel-version)
          pip -q download mlir==$VERSION \
            -f https://github.com/Xilinx/mlir-aie/releases/expanded_assets/mlir-distro
          unzip -q mlir-*.whl
          find mlir -exec touch -a -m -t 201108231405.14 {} \;

          popd

      - name: Install mlir-aie from wheel
        run: |
          source air-venv/bin/activate
          MLIR_AIE_VERSION=$(utils/clone-mlir-aie.sh --get-wheel-version)
          pip install mlir_aie==$MLIR_AIE_VERSION \
            -f https://github.com/Xilinx/mlir-aie/releases/expanded_assets/latest-wheels-4/

      - name: Get llvm-aie
        run: |
          source air-venv/bin/activate
          python3 -m pip install --upgrade --force-reinstall llvm-aie -f https://github.com/Xilinx/llvm-aie/releases/expanded_assets/nightly

      - name: Build mlir-air
        run: |
          source air-venv/bin/activate

          export PATH=$VITIS/bin:$VITIS/aietools/bin:$PATH
          source /opt/xilinx/xrt/setup.sh

          WHL_MLIR_DIR=$(pwd)/mlir-aie/mlir
          MLIR_AIE_INSTALL_DIR="$(python3 -m pip show mlir_aie | grep ^Location: | awk '{print $2}')/mlir_aie"
          PEANO_INSTALL_DIR=$(python3 -m pip show llvm-aie | grep ^Location: | awk '{print $2}')/llvm-aie
          CMAKEMODULES_DIR=$(pwd)/mlir-aie/cmake/modulesXilinx

          mkdir -p build_assert
          pushd build_assert

          sudo prlimit -lunlimited --pid $$

          cmake .. \
            -GNinja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DLLVM_ENABLE_ASSERTIONS=ON \
            -DPython3_EXECUTABLE=$(which python) \
            -DCMAKE_INSTALL_PREFIX=$PWD/../install \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_ASM_COMPILER=clang \
            -DCMAKE_EXE_LINKER_FLAGS_INIT="-fuse-ld=lld" \
            -DCMAKE_MODULE_LINKER_FLAGS_INIT="-fuse-ld=lld" \
            -DCMAKE_SHARED_LINKER_FLAGS_INIT="-fuse-ld=lld" \
            -DCMAKE_MODULE_PATH=${CMAKEMODULES_DIR}/ \
            -DLLVM_EXTERNAL_LIT=$(which lit) \
            -DLLVM_DIR=${WHL_MLIR_DIR}/lib/cmake/llvm \
            -DMLIR_DIR=${WHL_MLIR_DIR}/lib/cmake/mlir \
            -DAIE_DIR=${MLIR_AIE_INSTALL_DIR}/lib/cmake/aie \
            -Dx86_64_TOOLCHAIN_FILE=$PWD/../cmake/modules/toolchain_x86_64.cmake \
            -DAIR_RUNTIME_TARGETS:STRING="x86_64" \
            -DXRT_ROOT=/opt/xilinx/xrt \
            -DENABLE_RUN_XRT_TESTS=ON \
            -DPEANO_INSTALL_DIR=${PEANO_INSTALL_DIR}
          ninja install

          pip install $PWD/../python/spensor
          popd

      - name: Run benchmark (all models)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          # Force the classic HTTPS CDN download path. The hf_xet transfer
          # backend stalls at 0% on this runner (Xet CAS endpoint unreachable),
          # hanging the model fetch indefinitely.
          HF_HUB_DISABLE_XET: "1"
        run: |
          source air-venv/bin/activate

          export PATH=$VITIS/bin:$VITIS/aietools/bin:$PATH
          source /opt/xilinx/xrt/setup.sh

          # Raise memlock: the NPU driver mmaps ~64MB of locked memory; the
          # default RLIMIT_MEMLOCK makes that fail with EAGAIN, which breaks
          # device detection (xrt-smi/pyxrt) and NPU dispatch. Each GHA step is
          # a fresh shell, so this must be set here, not only in the build step.
          sudo prlimit -lunlimited --pid $$

          # Compile + inference run via `make` (not lit), so replicate the tool
          # env lit injects from the CMake build: PYTHONPATH + tool dirs on PATH.
          # Deliberately do NOT set LD_LIBRARY_PATH — env_setup.sh would place
          # LLVM/MLIR libs there, which the backend's `xrt-smi examine` subprocess
          # inherits and crashes on, silently falling back to npu1 (breaking ELF
          # output). air/mlir_aie libs resolve via RPATH, exactly as under lit.
          MLIR_AIE_DIR=$(python3 -m pip show mlir_aie | awk '/^Location:/{print $2}')/mlir_aie
          PEANO_DIR=$(python3 -m pip show llvm-aie | awk '/^Location:/{print $2}')/llvm-aie
          export MLIR_AIE_INSTALL_DIR="$MLIR_AIE_DIR"
          export PEANO_INSTALL_DIR="$PEANO_DIR"
          export PATH="$(pwd)/install/bin:$MLIR_AIE_DIR/bin:$PEANO_DIR/bin:$PATH"
          export PYTHONPATH="$(pwd)/install/python:$MLIR_AIE_DIR/python:/opt/xilinx/xrt/python${PYTHONPATH:+:$PYTHONPATH}"

          # Toolchain provenance stamped into every per-model perf record.
          MLIR_AIE_HASH=$(awk -F= '$1 ~ /(^| )HASH$/ {print $2; exit}' utils/clone-mlir-aie.sh)
          : "${MLIR_AIE_HASH:?HASH not found in utils/clone-mlir-aie.sh}"
          export BENCH_RUNNER=amdryzenai5pro340
          export BENCH_AIR_SHA="${{ github.sha }}"
          export BENCH_AIE_HASH="$MLIR_AIE_HASH"
          export BENCH_PEANO_VER=$(python3 -m pip show llvm-aie | awk '/^Version:/{print $2}')

          # Drive all models: compile -> verify -> profile -> per-model perf JSON.
          # Exits non-zero if any model's compile/verify failed (strict gate),
          # after processing every model so the run reports each one's status.
          bash programming_examples/llms/bench/run_models.sh "$BENCH_MODELS" "$PWD/perf_out"

      - name: Combine perf JSON
        if: always()
        run: |
          source air-venv/bin/activate
          python3 - <<'PY'
          import glob, json
          recs = [json.load(open(f)) for f in sorted(glob.glob("perf_out/*.perf.json"))]
          json.dump(recs, open("perf.json", "w"), indent=2)
          print(json.dumps(recs, indent=2))
          PY

      - name: Job summary
        if: always()
        run: |
          source air-venv/bin/activate
          python3 - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import glob, json
          recs = [json.load(open(f)) for f in sorted(glob.glob("perf_out/*.perf.json"))]
          print("### LLM perf benchmark\n")
          if not recs:
              print("**No results produced** — see step logs.")
          else:
              print("| model | verify | TTFT (ms) | tok/s |")
              print("| --- | --- | --- | --- |")
              for d in recs:
                  m = d["metrics"]
                  print(f"| {d['model']} | {d['verify_status'] or '?'} | "
                        f"{m['ttft_ms']} | {m['decode_tokens_per_sec']} |")
          PY

      - name: Upload perf artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-${{ github.run_id }}
          path: |
            perf.json
            perf_out/
